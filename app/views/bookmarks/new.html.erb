<h1>Bookmark a film</h1>
<h2>To list: <%= List.find(params[:list_id]).name %></h2>

<div class="container">
  <%= simple_form_for [@list, @bookmark] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie %>
  <%= f.button :submit, class: 'btn btn-primary' %>
<% end %>
</div>
